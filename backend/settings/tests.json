{
  "tests": [
    {
      "name": "Get all settings public",
      "method": "GET",
      "path": "/?action=all",
      "expectedStatus": 200,
      "expectedBody": {"settings": {}},
      "bodyMatcher": "partial"
    },
    {
      "name": "Save without auth",
      "method": "POST",
      "path": "/?action=save",
      "body": {"updates": [{"section": "general", "key": "phone", "value": "test"}]},
      "expectedStatus": 403,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Admin all without auth",
      "method": "GET",
      "path": "/?action=admin_all",
      "expectedStatus": 403,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}
