{
  "tests": [
    {
      "name": "Get stats without token",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 403,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}